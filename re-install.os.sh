#!/bin/bash
sudo apt-get install subversion subversion-tools
sudo apt-get install lighttpd apache2-utils
# trac0.12
# django should be to 1.3
sudo apt-get install  postgresql
#sdl psycopg2 
#for postgresql ?

